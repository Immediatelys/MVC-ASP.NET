@model ASP_MVC.Models.ProductModel

@{
    Layout = "_Layout";
}''<div class="card bg-danger text-white">
    <h1 class="card-header">@Model.Name</h1>
    <div class="card-body">
        <h5 class="card-title">Card title</h5>
        <p class="card-text">
            Giá: @Model.Price
        </p>
</div>